{
  "name": "chakram-poc",
  "version": "1.0.0",
  "description": "poc trial for chakram",
  "main": "main.js",
  "scripts": {
    "test": "NOW=$(date \"+%s\") && ./node_modules/.bin/mocha --reporter mochawesome --reporter-options reportFilename=test-${NOW}"
  },
  "author": "Ara Morales",
  "license": "ISC",
  "devDependencies": {
    "chakram": "^1.5.0",
    "mocha": "^5.0.1",
    "mochawesome": "^3.0.2"
  }
}
